name: Update Boletin Concursal

on:
  workflow_dispatch:
  schedule:
    - cron: "17 8 * * *"

permissions:
  contents: write

jobs:
  update-boletin:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: python -m pip install --upgrade requests

      - name: Download Boletin Concursal JSON
        run: python scripts/download_boletin_json.py

      - name: Commit and push changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add superir/publicaciones.json superir/metadata.json

          if git diff --cached --quiet; then
            echo "No hay cambios en los archivos generados."
            exit 0
          fi

          git commit -m "Update Boletin Concursal data"
          git push
